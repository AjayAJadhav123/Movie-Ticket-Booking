# Production: Leave VITE_BACKEND_URL empty so Vercel's proxy (vercel.json routes) handles
# all /api/* requests and forwards them to Render. Setting an absolute URL here would
# bypass the proxy and cause CORS errors if Render is on a different origin.
VITE_BACKEND_URL=

# Cashfree production mode
VITE_CASHFREE_MODE=production
