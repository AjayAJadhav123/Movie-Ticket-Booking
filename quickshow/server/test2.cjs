const jwt = require('jsonwebtoken'); const crypto = require('crypto'); const { verifyToken } = require('@clerk/express'); const { publicKey, privateKey } = crypto.generateKeyPairSync('rsa', { modulusLength: 2048, publicKeyEncoding: { type: 'spki', format: 'pem' }, privateKeyEncoding: { type: 'pkcs8', format: 'pem' } }); const payload = { sub: 'user_123', iss: 'https://peaceful-teal-4875.clerk.accounts.dev', azp: 'http://localhost:5174', iat: Math.floor(Date.now() / 1000), exp: Math.floor(Date.now() / 1000) + 3600 }; const token = jwt.sign(payload, privateKey, { algorithm: 'RS256' }); verifyToken(token, { jwtKey: publicKey, authorizedParties: ['http://localhost:5174'] }).then(console.log).catch(console.error);
